# Checkpoints

Place the real POM single-checkpoint weights here when available:

```text
checkpoints/pom_experiments_1_checkpoint2.pt
```

If the checkpoint is stored elsewhere, pass it explicitly:

```bash
python scripts/export_pom_reference.py --checkpoint /path/to/checkpoint2.pt
```
